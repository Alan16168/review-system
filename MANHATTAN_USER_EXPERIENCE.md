# 🎨 曼哈顿计划用户体验设计方案

**Review System Marketplace 用户购买后体验完整指南**

---

## 📋 文档概述

本文档详细说明用户在曼哈顿计划中购买产品后的完整使用体验，包括：
- 购买后引导流程（Onboarding）
- 三类产品的使用方式（模板、AI写作、AI视频）
- 信用点系统的用户体验
- UI/UX 设计规范
- 技术实现方案

**创建时间**: 2025-11-19  
**目标**: 让用户购买后能够轻松、便捷地使用所有功能

---

## 🎯 核心设计原则

### 1️⃣ **即时可用原则**
> 购买完成后，用户应该立即能够使用产品，无需额外配置

### 2️⃣ **渐进式引导原则**
> 不要一次性展示所有功能，根据用户操作逐步引导

### 3️⃣ **清晰反馈原则**
> 每个操作都要有明确的视觉反馈和状态提示

### 4️⃣ **零学习成本原则**
> 界面设计要直观，功能要自解释

---

## 📦 三类产品的使用流程设计

### 🎨 **产品一：付费模板**

#### **使用场景**：
用户购买了"产品经理年度复盘53问"模板（$19.99）

#### **完整流程**：

```
1. 购买完成 
   ↓
2. 立即显示使用入口（两个选择）
   ↓
3a. 选择：立即创建复盘          3b. 选择：稍后使用
    ↓                              ↓
4a. 跳转到创建页面              4b. 模板已保存到"我的模板库"
    - 自动选中已购买模板           - 随时可以从库中调用
    - 显示模板预览
    ↓
5a. 点击"使用此模板"
    ↓
6a. 自动创建新复盘
    - 标题：[2025年度产品经理复盘]
    - 问题：自动填充53个问题
    - 状态：草稿
    ↓
7a. 用户开始填写
```

#### **UI 设计 - 购买成功页面**：

```html
┌─────────────────────────────────────────────┐
│  ✅ 购买成功！                               │
│                                              │
│  📋 产品经理年度复盘53问                     │
│  💰 $19.99                                   │
│                                              │
│  ┌─────────────────────────────────────┐   │
│  │ 🎉 恭喜！此模板已添加到您的模板库    │   │
│  │                                      │   │
│  │ 您现在可以：                          │   │
│  │ • 立即创建一个新的复盘使用此模板      │   │
│  │ • 随时从"我的模板"中调用此模板        │   │
│  └─────────────────────────────────────┘   │
│                                              │
│  [🚀 立即使用模板]  [📚 查看我的模板库]      │
│                                              │
│  [< 返回 Marketplace]                        │
└─────────────────────────────────────────────┘
```

#### **UI 设计 - 创建复盘页面（模板已选中）**：

```html
┌─────────────────────────────────────────────┐
│  创建新复盘                                   │
│                                              │
│  选择模板                                     │
│  ┌─────────────────────────────────────┐   │
│  │ ✓ 产品经理年度复盘53问 [已购买]      │   │
│  │   深入反思产品管理的53个关键问题      │   │
│  │                                      │   │
│  │   [查看模板详情] [更换模板]           │   │
│  └─────────────────────────────────────┘   │
│                                              │
│  复盘标题                                     │
│  [2025年度产品经理复盘____________]          │
│                                              │
│  [📋 使用此模板创建]                         │
└─────────────────────────────────────────────┘
```

#### **数据库实现**：

```sql
-- 用户购买模板后，插入purchased_templates表
INSERT INTO purchased_templates (user_id, template_id, purchased_at)
VALUES (123, 45, CURRENT_TIMESTAMP);

-- 用户可见的"我的模板"包括：
-- 1. 免费模板（所有人可见）
-- 2. 已购买的付费模板
SELECT t.* FROM templates t
LEFT JOIN purchased_templates pt ON t.id = pt.template_id AND pt.user_id = 123
WHERE t.is_free = 1 OR pt.id IS NOT NULL;
```

---

### 🤖 **产品二：AI写作助手**

#### **使用场景**：
用户订阅了 Premium 套餐（$19.99/月，AI写作无限次）

#### **完整流程**：

```
用户场景：正在填写复盘问题
"请描述您今年最大的挑战？"
   ↓
1. 用户在输入框中简单输入："产品需求太多，优先级难决策"
   ↓
2. 点击输入框下方的 [✨ AI帮我扩写] 按钮
   ↓
3. 显示加载动画（2-5秒）
   ↓
4. AI生成内容显示在预览区域：
   "今年最大的挑战是面对海量的产品需求时，如何做出正确的优先级决策..."
   （约300-500字）
   ↓
5. 用户可以选择：
   - [✅ 采用此版本] → 替换原文本
   - [🔄 重新生成] → 再次调用AI
   - [✏️ 编辑后采用] → 修改后使用
   - [❌ 取消] → 保持原文本
```

#### **UI 设计 - AI写作助手界面**：

```html
┌─────────────────────────────────────────────┐
│  问题 1/53：请描述您今年最大的挑战？          │
│                                              │
│  您的回答                                     │
│  ┌─────────────────────────────────────┐   │
│  │ 产品需求太多，优先级难决策            │   │
│  │                                      │   │
│  │ [光标]                               │   │
│  └─────────────────────────────────────┘   │
│                                              │
│  AI助手功能                                  │
│  [✨ AI帮我扩写] [📝 AI润色] [🌐 翻译]       │
│                                              │
│  💡 Premium用户无限使用                      │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━       │
│                                              │
│  [保存草稿]  [下一题 →]                      │
└─────────────────────────────────────────────┘
```

#### **AI生成结果预览界面**：

```html
┌─────────────────────────────────────────────┐
│  ✨ AI生成内容（3秒）                         │
│                                              │
│  ┌─────────────────────────────────────┐   │
│  │ 📄 AI生成版本                        │   │
│  │ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━      │   │
│  │ 今年最大的挑战是面对海量的产品需求时，│   │
│  │ 如何做出正确的优先级决策。作为产品经理，│   │
│  │ 我们每天都会收到来自客户、销售团队、  │   │
│  │ 管理层等多方的需求输入，这些需求往往  │   │
│  │ 都声称"紧急且重要"。但实际上，资源有限，│   │
│  │ 开发团队的产能也有限...（300字）      │   │
│  └─────────────────────────────────────┘   │
│                                              │
│  [✅ 采用此版本]  [🔄 重新生成]              │
│  [✏️ 编辑后采用]  [❌ 取消]                  │
└─────────────────────────────────────────────┘
```

#### **使用量显示（信用点用户）**：

```html
┌─────────────────────────────────────────────┐
│  顶部通知栏                                   │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━       │
│  💰 信用点余额: 85 credits                   │
│  📊 本次AI写作消耗: 10 credits               │
│  🔔 余额不足时将无法使用AI功能                │
│  [💳 充值信用点]                             │
└─────────────────────────────────────────────┘
```

#### **API 实现**：

```typescript
// POST /api/ai/writing
app.post('/api/ai/writing', async (c) => {
  const { action, content } = await c.req.json();
  const user = c.get('user');
  
  // 1. 检查用户权限
  const canUse = await checkAIWritingPermission(user.id);
  if (!canUse) {
    return c.json({ error: '余额不足或订阅已过期' }, 403);
  }
  
  // 2. 调用 OpenAI API
  const prompt = generatePrompt(action, content);
  const result = await openai.chat.completions.create({
    model: 'gpt-4',
    messages: [{ role: 'user', content: prompt }]
  });
  
  // 3. 扣除费用
  await deductCredits(user.id, 'ai_writing', 10);
  
  // 4. 记录使用
  await logUsage(user.id, 'ai_writing', {
    action, input_length: content.length,
    output_length: result.choices[0].message.content.length
  });
  
  return c.json({
    generated_content: result.choices[0].message.content,
    credits_used: 10,
    credits_remaining: await getCreditsBalance(user.id)
  });
});
```

---

### 🎥 **产品三：AI视频生成**

#### **使用场景**：
用户写完复盘后，想生成培训视频分享给团队

#### **完整流程**：

```
1. 用户完成复盘填写
   ↓
2. 在复盘详情页，点击 [🎬 生成培训视频] 按钮
   ↓
3. 进入视频生成向导页面
   ↓
4. 步骤1：选择视频风格
   - 商务专业风格
   - 教育讲解风格
   - 创意动画风格
   ↓
5. 步骤2：选择配音语言
   - 中文（普通话）
   - 英文
   - 日文
   ↓
6. 步骤3：选择视频时长
   - 简版（3-5分钟，提取核心要点）
   - 完整版（8-12分钟，包含所有内容）
   ↓
7. 预览配置 & 确认生成
   ↓
8. 点击 [🚀 开始生成] 
   ↓
9. 显示生成进度
   - 分析文本... ✅
   - 生成脚本... ⏳ (30%)
   - 合成视频... ⏳
   - 添加配音... ⏳
   ↓
10. 生成完成通知（预计5-10分钟）
    - 邮件通知
    - 站内通知
    ↓
11. 视频预览 & 下载
```

#### **UI 设计 - 视频生成向导（步骤1）**：

```html
┌─────────────────────────────────────────────┐
│  🎬 AI视频生成向导                           │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━       │
│                                              │
│  步骤 1/3：选择视频风格                       │
│                                              │
│  ┌───────────┐ ┌───────────┐ ┌───────────┐ │
│  │ 💼 商务风格│ │ 🎓 教育风格│ │ 🎨 创意风格│ │
│  │           │ │           │ │           │ │
│  │[预览图片]  │ │[预览图片]  │ │[预览图片]  │ │
│  │           │ │           │ │           │ │
│  │ 专业、正式 │ │ 生动、易懂 │ │ 活泼、趣味 │ │
│  │ [✓ 选择]  │ │ [ 选择 ]  │ │ [ 选择 ]  │ │
│  └───────────┘ └───────────┘ └───────────┘ │
│                                              │
│  [< 返回]              [下一步 →]            │
└─────────────────────────────────────────────┘
```

#### **UI 设计 - 生成进度页面**：

```html
┌─────────────────────────────────────────────┐
│  🎬 视频生成中...                            │
│                                              │
│  ┌─────────────────────────────────────┐   │
│  │  预计剩余时间: 6分钟                  │   │
│  │                                      │   │
│  │  ████████████░░░░░░░░░░░░ 45%       │   │
│  └─────────────────────────────────────┘   │
│                                              │
│  当前进度：                                  │
│  ✅ 分析文本内容 (完成)                      │
│  ✅ 生成视频脚本 (完成)                      │
│  ⏳ 合成视频画面 (进行中...)                 │
│  ⏸️ 添加AI配音 (等待中)                      │
│  ⏸️ 添加字幕 (等待中)                        │
│  ⏸️ 最终渲染 (等待中)                        │
│                                              │
│  💡 提示：视频生成需要5-10分钟，您可以先去做  │
│     其他事情，我们会通过邮件通知您完成。      │
│                                              │
│  [❌ 取消生成]  [最小化到后台]               │
└─────────────────────────────────────────────┘
```

#### **UI 设计 - 视频完成页面**：

```html
┌─────────────────────────────────────────────┐
│  ✅ 视频生成完成！                           │
│                                              │
│  ┌─────────────────────────────────────┐   │
│  │                                      │   │
│  │       [视频播放器预览]                │   │
│  │                                      │   │
│  │     时长: 8:32  |  大小: 42.5 MB     │   │
│  └─────────────────────────────────────┘   │
│                                              │
│  视频信息                                     │
│  • 标题：2025年度产品经理复盘培训              │
│  • 风格：商务专业风格                         │
│  • 语言：中文（普通话）                       │
│  • 生成时间：2025-11-19 15:30                │
│                                              │
│  操作                                         │
│  [⬇️ 下载视频]  [🔗 获取分享链接]            │
│  [✏️ 重新生成]  [📧 发送到邮箱]              │
│                                              │
│  消耗信用点：300 credits                      │
│  当前余额：450 credits                        │
│                                              │
│  [< 返回我的复盘]  [生成更多视频]             │
└─────────────────────────────────────────────┘
```

#### **后台任务实现**：

```typescript
// POST /api/ai/video
app.post('/api/ai/video', async (c) => {
  const { review_id, style, language, duration } = await c.req.json();
  const user = c.get('user');
  
  // 1. 检查权限和余额
  const canGenerate = await checkVideoGenerationPermission(user.id);
  if (!canGenerate) {
    return c.json({ error: '余额不足或超出配额' }, 403);
  }
  
  // 2. 创建异步任务
  const task = await createVideoTask({
    user_id: user.id,
    review_id,
    style,
    language,
    duration,
    status: 'queued'
  });
  
  // 3. 扣除费用（预扣）
  await deductCredits(user.id, 'ai_video', 300);
  
  // 4. 加入任务队列（后台处理）
  await queueVideoGeneration(task.id);
  
  return c.json({
    task_id: task.id,
    estimated_time: '5-10分钟',
    credits_used: 300
  });
});

// 后台队列处理（使用 Cloudflare Queues）
async function processVideoGeneration(task_id) {
  // 1. 更新状态：分析文本
  await updateTaskStatus(task_id, 'analyzing', 10);
  
  // 2. 提取复盘内容
  const review = await getReviewContent(task.review_id);
  
  // 3. 生成视频脚本（调用 GPT API）
  await updateTaskStatus(task_id, 'scripting', 30);
  const script = await generateVideoScript(review.content);
  
  // 4. 调用视频生成API（Synthesia/D-ID）
  await updateTaskStatus(task_id, 'rendering', 50);
  const video_url = await synthesiaAPI.generateVideo({
    script,
    style: task.style,
    language: task.language
  });
  
  // 5. 生成字幕
  await updateTaskStatus(task_id, 'subtitles', 80);
  const subtitles = await generateSubtitles(script);
  
  // 6. 完成
  await updateTaskStatus(task_id, 'completed', 100);
  await saveVideoResult(task_id, video_url, subtitles);
  
  // 7. 发送通知
  await sendNotification(task.user_id, {
    type: 'video_completed',
    task_id,
    video_url
  });
}
```

---

## 💳 信用点系统用户体验设计

### **核心设计目标**：
1. **余额始终可见** - 让用户随时知道自己还有多少credits
2. **消耗透明** - 每次使用前明确告知消耗量
3. **充值便捷** - 余额不足时一键充值
4. **使用清晰** - 详细的使用历史记录

---

### **UI 设计 - 全局余额显示（顶部导航栏）**：

```html
┌─────────────────────────────────────────────┐
│  Review System          [搜索框]             │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━       │
│  [复盘] [Marketplace] [我的模板]             │
│                                              │
│                          💰 850 Credits     │
│                          [+充值]  [👤 Alan] │
└─────────────────────────────────────────────┘
```

---

### **UI 设计 - 信用点管理页面**：

```html
┌─────────────────────────────────────────────┐
│  💰 信用点管理                               │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━       │
│                                              │
│  当前余额                                     │
│  ┌─────────────────────────────────────┐   │
│  │         850 Credits                  │   │
│  │                                      │   │
│  │  ┌──────────────────────┐           │   │
│  │  │ ████████████░░░░░░░░ │ 60%       │   │
│  │  └──────────────────────┘           │   │
│  │                                      │   │
│  │  本月已使用: 650 / 1500              │   │
│  │  [💳 购买更多Credits]                │   │
│  └─────────────────────────────────────┘   │
│                                              │
│  购买信用点套餐                               │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐   │
│  │ Starter  │ │ Popular  │ │ Pro Pack │   │
│  │ 100      │ │ 600 ⭐   │ │ 2600 ⭐  │   │
│  │ Credits  │ │ Credits  │ │ Credits  │   │
│  │          │ │ 送20%    │ │ 送30%    │   │
│  │ $9.99    │ │ $39.99   │ │ $149.99  │   │
│  │ [购买]   │ │ [购买]   │ │ [购买]   │   │
│  └──────────┘ └──────────┘ └──────────┘   │
│                                              │
│  本月使用统计                                 │
│  ┌─────────────────────────────────────┐   │
│  │ 📊 使用明细                          │   │
│  │ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━      │   │
│  │ AI写作: 340 credits (52%)            │   │
│  │ AI视频: 300 credits (46%)            │   │
│  │ 模板购买: 10 credits (2%)            │   │
│  │                                      │   │
│  │ [查看详细历史]                        │   │
│  └─────────────────────────────────────┘   │
│                                              │
│  💡 智能建议                                 │
│  根据您的使用习惯，我们推荐您订阅Premium套餐  │
│  ($19.99/月)，可节省约60%的费用。            │
│  [查看订阅套餐]                              │
└─────────────────────────────────────────────┘
```

---

### **UI 设计 - 使用历史记录**：

```html
┌─────────────────────────────────────────────┐
│  📜 信用点使用历史                           │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━       │
│                                              │
│  筛选条件                                     │
│  [全部类型 ▾] [最近30天 ▾] [搜索...]         │
│                                              │
│  ┌─────────────────────────────────────┐   │
│  │ 2025-11-19 15:30                     │   │
│  │ 🎥 AI视频生成                        │   │
│  │ "2025年度产品经理复盘"               │   │
│  │ -300 credits  |  余额: 850           │   │
│  └─────────────────────────────────────┘   │
│                                              │
│  ┌─────────────────────────────────────┐   │
│  │ 2025-11-19 14:15                     │   │
│  │ ✨ AI写作助手                        │   │
│  │ "扩写：项目挑战描述"                  │   │
│  │ -10 credits   |  余额: 1150          │   │
│  └─────────────────────────────────────┘   │
│                                              │
│  ┌─────────────────────────────────────┐   │
│  │ 2025-11-18 10:20                     │   │
│  │ 💳 购买信用点                        │   │
│  │ Popular Pack (送20%)                 │   │
│  │ +600 credits  |  余额: 1160          │   │
│  └─────────────────────────────────────┘   │
│                                              │
│  [加载更多]                                  │
│  [📥 导出Excel报表]                          │
└─────────────────────────────────────────────┘
```

---

### **余额不足提醒设计**：

#### **场景1：使用AI功能时余额不足**

```html
┌─────────────────────────────────────────────┐
│  ⚠️ 信用点余额不足                           │
│                                              │
│  当前余额: 5 credits                         │
│  需要: 10 credits (AI写作)                   │
│  差额: 5 credits                             │
│                                              │
│  ┌─────────────────────────────────────┐   │
│  │ 快速充值                              │   │
│  │ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━      │   │
│  │ [ ] Starter Pack - 100 credits      │   │
│  │     $9.99                            │   │
│  │                                      │   │
│  │ [✓] Popular Pack - 600 credits ⭐   │   │
│  │     $39.99 (推荐)                    │   │
│  └─────────────────────────────────────┘   │
│                                              │
│  [💳 立即充值]  [取消]                       │
│                                              │
│  💡 或者订阅Premium套餐，AI写作无限使用！      │
│  [查看订阅套餐]                              │
└─────────────────────────────────────────────┘
```

#### **场景2：余额低于20%时的顶部提醒**

```html
┌─────────────────────────────────────────────┐
│  ⚠️ 提醒：您的信用点余额不足20% (仅剩150)     │
│  为避免影响使用，建议尽快充值 [充值] [关闭]   │
└─────────────────────────────────────────────┘
```

---

## 🎓 购买后引导流程（Onboarding）

### **目标**：让新用户快速了解如何使用已购买的功能

### **引导时机**：
1. **首次购买后** - 立即触发引导
2. **订阅升级后** - 介绍新功能
3. **用户主动请求** - 提供帮助中心

---

### **引导流程设计**：

```
1. 购买完成页
   ↓
2. 显示欢迎弹窗
   "🎉 欢迎成为Premium会员！让我们花30秒了解如何使用新功能"
   [开始引导] [跳过]
   ↓
3. 引导步骤1：模板库
   高亮显示"我的模板"菜单
   "这里是您已购买的所有模板，随时可以使用"
   [下一步]
   ↓
4. 引导步骤2：AI写作
   高亮显示AI助手按钮
   "在任何输入框中，点击AI按钮即可使用写作助手"
   [下一步]
   ↓
5. 引导步骤3：AI视频
   高亮显示视频生成按钮
   "完成复盘后，一键生成培训视频分享给团队"
   [下一步]
   ↓
6. 引导步骤4：信用点（如适用）
   高亮显示顶部余额显示
   "这里显示您的信用点余额，随时查看使用情况"
   [完成引导]
   ↓
7. 引导完成
   "✅ 准备就绪！开始创建您的第一个复盘吧"
   [立即开始]
```

---

### **UI 设计 - 引导弹窗（步骤1）**：

```html
┌─────────────────────────────────────────────┐
│  🎉 欢迎成为Premium会员！                     │
│                                              │
│  让我们花30秒时间，快速了解如何使用新功能      │
│                                              │
│  [开始引导]  [稍后再说]                       │
└─────────────────────────────────────────────┘
```

---

### **UI 设计 - 引导高亮（步骤2）**：

```html
┌─────────────────────────────────────────────┐
│  Review System                               │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━       │
│  [复盘] [Marketplace] ➡️[📚 我的模板]⬅️      │
│                               ▲              │
│         ┌──────────────────────┘             │
│         │  💡 步骤 1/4                        │
│         │  这里是您已购买的所有模板            │
│         │  随时可以使用创建新复盘              │
│         │                                    │
│         │  [下一步]  [跳过引导]               │
│         └────────────────────────            │
└─────────────────────────────────────────────┘
```

---

## 📱 响应式设计考虑

### **移动端适配**：

#### **模板购买后（移动端）**：

```html
┌────────────────────┐
│ ✅ 购买成功！       │
│                    │
│ 📋 产品经理年度复盘│
│    53问            │
│ 💰 $19.99          │
│                    │
│ ┌────────────────┐│
│ │ 🎉 模板已添加   ││
│ │    到您的库中   ││
│ └────────────────┘│
│                    │
│ [🚀 立即使用]      │
│ [📚 我的模板库]    │
│                    │
│ [< 返回]           │
└────────────────────┘
```

#### **AI写作（移动端）**：

```html
┌────────────────────┐
│ 问题1: 最大挑战？   │
│                    │
│ ┌────────────────┐│
│ │产品需求太多，   ││
│ │优先级难决策     ││
│ └────────────────┘│
│                    │
│ [✨ AI扩写]        │
│ [📝 AI润色]        │
│                    │
│ 💰 10 credits/次   │
│ 余额: 850          │
│                    │
│ [保存] [下一题→]   │
└────────────────────┘
```

---

## 🔔 通知系统设计

### **通知类型**：

1. **购买成功通知**
   - 站内消息 + 邮件
   - 内容：购买详情、使用指南链接

2. **视频生成完成通知**
   - 站内消息 + 邮件 + 推送
   - 内容：视频预览链接、下载链接

3. **余额不足提醒**
   - 站内消息
   - 内容：当前余额、充值链接

4. **订阅到期提醒**
   - 邮件（提前7天、3天、1天）
   - 内容：续费链接、订阅历史

5. **每月账单通知**
   - 邮件（每月1号）
   - 内容：上月使用统计、费用明细

---

### **UI 设计 - 通知中心**：

```html
┌─────────────────────────────────────────────┐
│  🔔 通知中心                                 │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━       │
│                                              │
│  今天                                         │
│  ┌─────────────────────────────────────┐   │
│  │ 🎥 视频生成完成                      │   │
│  │ "2025年度产品经理复盘"视频已生成      │   │
│  │ [查看视频]                           │   │
│  │ 15分钟前                             │   │
│  └─────────────────────────────────────┘   │
│                                              │
│  ┌─────────────────────────────────────┐   │
│  │ 💰 信用点余额提醒                    │   │
│  │ 您的余额不足20%，建议充值             │   │
│  │ [立即充值]                           │   │
│  │ 2小时前                              │   │
│  └─────────────────────────────────────┘   │
│                                              │
│  昨天                                         │
│  ┌─────────────────────────────────────┐   │
│  │ ✅ 购买成功                          │   │
│  │ 您已购买"产品经理年度复盘53问"模板    │   │
│  │ [查看详情]                           │   │
│  │ 昨天 14:30                           │   │
│  └─────────────────────────────────────┘   │
│                                              │
│  [标记全部已读]  [清空通知]                  │
└─────────────────────────────────────────────┘
```

---

## 💾 技术实现要点

### **1. 购买状态管理**

```typescript
// 检查用户是否已购买模板
async function hasUserPurchasedTemplate(userId: number, templateId: number) {
  const purchase = await env.DB.prepare(`
    SELECT id FROM purchased_templates 
    WHERE user_id = ? AND template_id = ?
  `).bind(userId, templateId).first();
  
  return !!purchase;
}

// 检查用户订阅状态
async function getUserSubscriptionTier(userId: number) {
  const subscription = await env.DB.prepare(`
    SELECT tier FROM subscriptions 
    WHERE user_id = ? AND status = 'active'
  `).bind(userId).first();
  
  return subscription?.tier || 'free';
}
```

---

### **2. AI功能权限检查**

```typescript
// 检查AI写作权限
async function checkAIWritingPermission(userId: number) {
  const tier = await getUserSubscriptionTier(userId);
  
  // Premium及以上用户无限使用
  if (['premium', 'team', 'enterprise'].includes(tier)) {
    return { allowed: true, unlimited: true };
  }
  
  // 免费用户检查信用点
  const credits = await getCreditsBalance(userId);
  if (credits >= 10) {
    return { allowed: true, unlimited: false, credits_required: 10 };
  }
  
  return { allowed: false, reason: 'insufficient_credits' };
}

// 检查AI视频权限
async function checkAIVideoPermission(userId: number) {
  const tier = await getUserSubscriptionTier(userId);
  
  // 企业用户无限使用
  if (tier === 'enterprise') {
    return { allowed: true, unlimited: true };
  }
  
  // Premium/Team用户检查配额
  if (['premium', 'team'].includes(tier)) {
    const quota = await getMonthlyVideoQuota(userId);
    if (quota.used < quota.limit) {
      return { 
        allowed: true, 
        unlimited: false, 
        remaining: quota.limit - quota.used 
      };
    }
    return { allowed: false, reason: 'quota_exceeded' };
  }
  
  // 免费用户检查信用点
  const credits = await getCreditsBalance(userId);
  if (credits >= 300) {
    return { allowed: true, unlimited: false, credits_required: 300 };
  }
  
  return { allowed: false, reason: 'insufficient_credits' };
}
```

---

### **3. 信用点事务处理**

```typescript
// 扣除信用点（事务安全）
async function deductCredits(
  userId: number, 
  productType: string, 
  amount: number
) {
  // 1. 开始事务
  await env.DB.prepare('BEGIN TRANSACTION').run();
  
  try {
    // 2. 锁定用户信用点记录
    const userCredits = await env.DB.prepare(`
      SELECT balance FROM user_credits 
      WHERE user_id = ? FOR UPDATE
    `).bind(userId).first();
    
    // 3. 检查余额
    if (userCredits.balance < amount) {
      throw new Error('Insufficient credits');
    }
    
    // 4. 扣除余额
    const newBalance = userCredits.balance - amount;
    await env.DB.prepare(`
      UPDATE user_credits 
      SET balance = ?, total_used = total_used + ?, last_updated = CURRENT_TIMESTAMP
      WHERE user_id = ?
    `).bind(newBalance, amount, userId).run();
    
    // 5. 记录交易
    await env.DB.prepare(`
      INSERT INTO credit_transactions 
      (user_id, amount, type, product_type, balance_after, created_at)
      VALUES (?, ?, 'usage', ?, ?, CURRENT_TIMESTAMP)
    `).bind(userId, -amount, productType, newBalance).run();
    
    // 6. 提交事务
    await env.DB.prepare('COMMIT').run();
    
    return { success: true, new_balance: newBalance };
    
  } catch (error) {
    // 7. 回滚事务
    await env.DB.prepare('ROLLBACK').run();
    throw error;
  }
}
```

---

### **4. 异步任务队列（视频生成）**

```typescript
// 使用 Cloudflare Queues
interface VideoGenerationMessage {
  task_id: string;
  user_id: number;
  review_id: number;
  style: string;
  language: string;
  duration: string;
}

// 发送到队列
export default {
  async fetch(request: Request, env: Env) {
    // ... 创建任务后
    await env.VIDEO_QUEUE.send({
      task_id: task.id,
      user_id: user.id,
      review_id: review.id,
      style: 'business',
      language: 'zh-CN',
      duration: 'full'
    });
  }
}

// 队列消费者
export default {
  async queue(batch: MessageBatch<VideoGenerationMessage>, env: Env) {
    for (const message of batch.messages) {
      try {
        await processVideoGeneration(message.body, env);
        message.ack();
      } catch (error) {
        message.retry();
      }
    }
  }
}
```

---

## 📊 用户体验指标（UX Metrics）

### **关键指标监控**：

1. **购买转化率**
   - 目标：30%以上
   - 监控：访问Marketplace → 购买完成

2. **首次使用时间（Time to First Use）**
   - 目标：购买后5分钟内
   - 监控：购买完成 → 首次使用产品

3. **功能发现率**
   - 目标：80%用户在首周发现所有功能
   - 监控：引导完成率、功能使用率

4. **AI功能使用率**
   - 目标：60%付费用户每周使用AI
   - 监控：AI写作/视频生成次数

5. **信用点消耗率**
   - 目标：70%以上
   - 监控：已购信用点 vs 已使用信用点

6. **客户满意度（CSAT）**
   - 目标：4.5/5.0
   - 监控：使用后反馈评分

---

## 🎯 总结：用户便捷性核心要素

### ✅ **立即可用**
- 购买完成 = 功能解锁
- 无需等待、配置、审核

### ✅ **清晰引导**
- 首次购买提供30秒快速引导
- 随时可访问帮助中心

### ✅ **透明消费**
- 使用前明确告知费用
- 实时显示余额和使用情况

### ✅ **智能提醒**
- 余额不足主动提醒
- 推荐最优套餐

### ✅ **便捷操作**
- 一键充值
- 一键生成视频
- 一键使用AI

### ✅ **状态反馈**
- 每个操作都有明确反馈
- 异步任务显示进度

---

## 📞 下一步行动

### **需要实现的核心功能**：

1. **Phase 1（2周内）**
   - [ ] 购买成功页面
   - [ ] 模板库集成
   - [ ] 信用点余额显示
   - [ ] 基础通知系统

2. **Phase 2（4周内）**
   - [ ] AI写作助手集成
   - [ ] 引导流程实现
   - [ ] 信用点管理页面
   - [ ] 使用历史记录

3. **Phase 3（6周内）**
   - [ ] AI视频生成
   - [ ] 异步任务队列
   - [ ] 完整通知系统
   - [ ] UX指标监控

---

**文档版本**: v1.0  
**创建时间**: 2025-11-19  
**相关文档**: MANHATTAN_PROJECT.md  
**负责人**: Alan
